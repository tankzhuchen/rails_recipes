
<nav class="navbar navbar-default navbar-fixed-top " role="navigation">
    <div class="container-fluid">

        <!-- Brand and toggle get grouped for better mobile display  -->
        <div class="navbar-header">
            <a class="navbar-brand" href="/"><img src='<%= image_url 'logo.png' %>' >基础油首页 </a>
            <div class="navbar-header">
              <button type="button" class="btn btn-default navbar-btn "><%= link_to("油知识 | 资讯", posts_path, :class => "color") %> </button>
              <button type="button" class="btn btn-default navbar-btn "><%= link_to("看报价", jgs_path, :class => "color") %> </button>

            </div>

        </div>


        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse navbar-ex1-collapse" id="bs-example-navbar-collapse-1">






            <ul class="nav navbar-nav navbar-right">


                              <% if !current_user %>

                <% else %>
                  <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                        Hi!, <%= current_user.email %>
                        <b class="caret"></b>
                    </a>
                    <ul class="dropdown-menu">
                                  <% if current_user.admin? %>
              <li>
                 <%= link_to("文章后台", admin_posts_path ) %>
                 <%= link_to("报价后台", admin_jgs_path ) %>
                  <%= link_to("物流后台", admin_wls_path ) %>
               </li>
             <% end %>
                        <li> <%= link_to(content_tag(:i, '登出', class: 'fa fa-sign-out'), destroy_user_session_path, method: :delete) %> </li>
                    </ul>
                  </li>
                <% end %>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
</nav>
