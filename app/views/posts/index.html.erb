

   <div class="col-md-12 ">
     <div class="container">
     <div id="post-list">
   <% @posts.each do |post| %>

    <%= render :partial => "post", :locals => { :post => post } %>

  <% end %>

    </div>

<%= paginate @posts %>

      </div>

        </div>
